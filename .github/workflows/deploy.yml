name: Deploy Vite App to Server

on:
  push:
    branches:
      - main
      - dev
      - testing

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 23

      - name: Install dependencies
        run: npm install

      - name: Build Vite app
        run: npm run build

      - name: Install sshpass
        run: sudo apt-get update && sudo apt-get install -y sshpass

      - name: Deploy to server via rsync with password
        env:
          SSH_PASSWORD: ${{ secrets.SSH_PASSWORD }}
        run: |
          # Choose server and remote dir based on branch
          SERVER="122.186.126.218"
          if [[ $GITHUB_REF == 'refs/heads/main' ]]; then
            REMOTE_DIR="/home/codoid/sathish_property/live_deploy"
          elif [[ $GITHUB_REF == 'refs/heads/dev' ]]; then
            REMOTE_DIR="/home/codoid/sathish_property/dev_deploy"
          elif [[ $GITHUB_REF == 'refs/heads/testing' ]]; then
            REMOTE_DIR="/home/codoid/sathish_property/deploy_test"
          else
            echo "Branch not configured for deployment"
            exit 1
          fi

          echo "🚀 Deploying to $SERVER:$REMOTE_DIR"

          # Backup existing build
          sshpass -p "$SSH_PASSWORD" ssh -o StrictHostKeyChecking=no codoid@$SERVER "
            if [ -d $REMOTE_DIR ]; then
              TIMESTAMP=\$(date +%Y%m%d-%H%M%S)
              BACKUP_DIR=\${REMOTE_DIR}_backup_\$TIMESTAMP
              cp -r $REMOTE_DIR \$BACKUP_DIR
              echo '✅ Backup created at \$BACKUP_DIR'
            else
              echo 'ℹ️ No existing build found to backup'
            fi
          "

          # Deploy dist folder
          sshpass -p "$SSH_PASSWORD" rsync -avz --delete -e "ssh -o StrictHostKeyChecking=no" dist/ codoid@$SERVER:$REMOTE_DIR

          echo "✅ Deployment finished!"
